[project]
name = "a5-nodes"
description = "A collection of A5 prompt, text, image, and latent nodes for ComfyUI."
version = "0.1.1"
readme = "README.md"
requires-python = ">=3.10"
license = { file = "LICENSE" }
authors = [{ name = "A5Projects" }]
dependencies = []

[project.urls]
Repository = "https://github.com/A5Projects/ComfyUI-A5Nodes"

[tool.comfy]
PublisherId = "a5projects"
DisplayName = "A5 Custom Nodes"
