[project]
name = "a5-nodes"
description = "A collection of A5 prompt, text, image, and latent nodes for ComfyUI."
version = "0.1.0"
readme = "README.md"
requires-python = ">=3.10"
license = { file = "LICENSE" }
authors = [{ name = "A5" }]
dependencies = []

[tool.comfy]
# Replace this placeholder with the immutable Comfy Registry publisher ID before publishing.
PublisherId = "TODO"
DisplayName = "A5 Nodes"
